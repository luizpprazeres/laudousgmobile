# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
build/
.next/
.turbo/
.expo/
.expo-shared/
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Coverage
coverage/
.nyc_output/

# Drizzle
drizzle/meta/

# Expo
ios/
android/
web-build/

# Vercel
.vercel

# Conteúdo extraído do LaudoUSG original (proprietário, dados clínicos
# anonimizados — manter LOCAL, fora de repos públicos).
# Inclui: prompts originais, modelos, regras, frases, few-shots,
# laudos reais anonimizados de 30 dias.
_extraction/

# Golden validation outputs (por run, não persistem)
scripts/.golden-reports/
tests/golden-objetivo/report.html
tests/golden-deterministico/report.html

# artefatos temporários de review (contêm dados de paciente — nunca commitar)
tmp-review/

# Rascunhos de sessão: harnesses de benchmark, relatórios de validação A/B,
# scripts ad-hoc. Podem conter trechos de laudo real — manter LOCAL.
# Se algo aqui virar ferramenta de verdade, promover para scripts/ ou tests/.
scratchpad/

# Material de estudo local (PDFs de livros — nunca commitar)
study/

# Config local do agente Codex
.codex/

# Smoke scripts locais ad-hoc
apps/api/_smoke*.mjs

# Harness experimental do Writer V2 (descartável)
apps/api/_writer_v2_harness.*

# Áudio do benchmark de ASR — ~10 MB de .wav com ditado de teste.
# Estava fora do git só por nunca ter sido adicionado: um `git add -A`
# distraído o traria junto. Repositório PÚBLICO, então a proteção é explícita.
# (Achado do @devops na auditoria do deploy de 11/08.)
scripts/asr-bench/audio/
scripts/asr-bench/bench_results.json
scripts/asr-bench/keyterms.json
