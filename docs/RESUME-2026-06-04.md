# RESUME — LaudoUSG c1 (sessão 2026-06-03→04)

> Doc de retomada após restart do terminal (maestri CLI caiu na virada do dia — temp purgado).
> Ao reabrir: rodar `maestri list` pra confirmar dex1, depois seguir as tarefas abaixo.

## ✅ Entregue HOJE (tudo em prod/commitado)

| Item | Commit | Estado |
|---|---|---|
| **S27** Modo Objetivo enumerativo (backend) + S27.1 fix IG obstétrica | `d7f196a` | deploy prod ✅ |
| **S28** Onboarding hand-holding (iOS) | `7c34861` | commitado iOS (NÃO pushado — entra no próximo archive/TestFlight) |
| **BUG PELVE** via de acesso (saía sempre "TA e TV") | `104f902` | deploy prod ✅ |
| **BUG líquido amniótico** sobrescrevendo classificação do médico | `c40483e` | deploy prod ✅ |
| **FIX A magnitude** `[REVISAR]` espúrio em biometria normal | `7a191a3` | deploy prod ✅ verificado 8/8 |

Todos os fixes de backend estão LIVE pra todos os usuários (web+app). S28 (iOS) só chega aos usuários num novo build.

## 🔎 Auditoria de laudos reais — achados
- **Placeholders `____` = INTENCIONAIS** (decisão do Luiz, NÃO bug). Ver memory `project_placeholders_intencionais`.
- **Fix B (conclusão Doppler) — PENDENTE**. Spec clínica completa do Luiz na memory `project_doppler_obstetrico_spec`.
- **Fix C (morfológico+Doppler vira obstétrico) — PENDENTE, GRAVE** (Luiz: "não tô conseguindo fazer os morfológicos"). Causa: categoria resolve p/ DOPPLER_OBSTETRICO. Mesma memory.

## 🎯 PRÓXIMA TAREFA: decidir B ou C
- **C** desbloqueia os morfológicos (Luiz disse estar travado) — roteamento de categoria + composição template morfo + seção Doppler.
- **B** = conclusão Doppler: umbilical/ACM **manual** (default normal), uterinas **auto** (incisura/>P95), campos opcionais ducto/RCP/**perfil=1/RCP**, separar o bug D2 (umbilical >P95 ditado caía na frase das uterinas).
- Perguntar ao Luiz qual atacar primeiro.

## 📚 Ler pra recuperar contexto (ordem)
1. **memory `project_doppler_obstetrico_spec`** — CRÍTICO (spec B + C, fraseologia exata)
2. memory `project_placeholders_intencionais`, `feedback_writing_style_overlay_limit` (padrão prompt-não-vence → determinístico)
3. Este doc
4. `docs/ARCHITECTURE.md` (repo iOS) §14
5. note `next-sprints` no canvas (quando maestri voltar) — **ATUALIZAR como 1ª tarefa**

## 🛠️ Padrão de fix validado 3x hoje (PELVE, líquido, magnitude)
Detectar sinal no `raw_input` por regex + filtrar/reescrever no `route.ts` **pós-writer** (determinístico — prompt sozinho não vence regras conflitantes, lição recorrente). Validar com **função pura testável** + **end-to-end vs prod**. Pedir **dex1 review** (pegou bugs reais 2x: negação de abortamento na PELVE, negação de oligoâmnio no líquido).

Guards criados em `apps/api/src/server/pipeline/`: `pelveRouteSelection.ts`, `amnioticFluidGuard.ts`, `magnitudeGuard.ts` — todos chamados em `route.ts` após o writer.

## ⚙️ Credenciais de teste (validação local/prod)
- JWT: password grant em `yldtkqrsbgcnwlydrrot.supabase.co/auth/v1/token` com `luizp02121@gmail.com`/`teste123` + anon key (em AppConfig.swift / .env do monorepo).
- Estilo CLASSICO = `11111111-1111-4111-8111-111111111111`; OBJETIVO = `44444444-...`.
- Backend local: `pnpm api:dev` (porta 3000, usa prod Supabase via DATABASE_URL). Prod: `laudousgmobile.vercel.app`.
